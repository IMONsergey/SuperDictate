import SuperDictateUI

/// Keeps the monolithic runtime source independent from file-scoped UI imports
/// while the control-panel integration is being strangled into separate files.
typealias SuperDictateInterfaceLanguage = SuperDictateUI.SuperDictateInterfaceLanguage
